# Files permitted to construct http.Transport / websocket.Dialer literals,
# or to assign a .Proxy field, outside the egress guard helpers.
#
# A listed file may construct a transport/dialer literal only if it guards
# the value (egress.GuardTransport / egress.GuardWebsocketDialer /
# egress.RoundTripper), and may assign a .Proxy field only through
# egress.WrapProxyFunc.
#
# internal/pluginhost/http_bridge.go is allowed to assign .Proxy without a
# direct egress.GuardTransport call because plugins cannot load under
# `locked` (Task 8); its .Proxy assignment still goes through
# egress.WrapProxyFunc.
#
# internal/api/handlers/management/plugin_quota.go builds an http.Client whose
# Transport is h.apiCallTransport(...), the guarded transport from api_tools.go.
# It never constructs a transport of its own.
sdk/proxyutil/proxy.go
internal/runtime/executor/antigravity_executor.go
internal/runtime/executor/helps/utls_client.go
internal/auth/claude/utls_transport.go
internal/api/handlers/management/api_tools.go
internal/api/handlers/management/plugin_quota.go
internal/runtime/executor/codex_websockets_connection.go
internal/client/codex/live/sideband.go
internal/pluginhost/http_bridge.go
internal/egress/transport.go
#
# The files below match the raw-dial / third-party-client part of the scan.
# internal/home/client.go dials Redis and the Home server through its own
# dialers and internal/home/certificate.go dials the Home host for its
# certificate bootstrap; internal/store/{gitstore,objectstore,postgresstore}.go use go-git,
# minio and pgx. None passes the gate, so the locked build refuses Home mode
# and those stores at startup (cmd/server/locked_refusal.go) and vetoes
# home.enabled on reload. internal/util/ssh_helper.go does a UDP connect to
# learn the local route; a UDP connect sends no packet. helps/utls_client.go
# wraps an http2.Transport around a connection its guarded http.Transport
# already dialed.
internal/home/client.go
internal/home/certificate.go
internal/store/gitstore.go
internal/store/objectstore.go
internal/store/postgresstore.go
internal/util/ssh_helper.go
